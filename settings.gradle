dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Flickr by DxC"
include ':app', ':domain', ':usecase', ':data', ':datasource', ':commons', ':presentation'
